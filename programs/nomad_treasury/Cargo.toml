[package]
name = "nomad-treasury"
version = "0.1.0"
description = "Nomad AI's on-chain treasury and bounty system"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "nomad_treasury"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"
